<%= form_for [@restaurant, @review] do |f| %>
		<%= f.label :thoughts %>
		<%= f.text_area :thoughts %><br>
		<%= f.label :rating %>
		<%= f.select :rating, Review::VALID_RATINGS %><br>
	
		<%= f.submit 'Leave review' %>
<% end %>